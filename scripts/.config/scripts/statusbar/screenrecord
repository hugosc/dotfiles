#!/usr/bin/env bash

# dwmblocks status for screen recording
# Shows black dot when recording, empty otherwise (respects separators)

if ~/.config/scripts/screenrecord status >/dev/null 2>&1; then
	echo "●"
fi
